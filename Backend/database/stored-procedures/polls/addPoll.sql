USE CONNECT360;
GO

CREATE OR ALTER PROCEDURE AddPoll(
   @ID VARCHAR(255),
   @TITLE VARCHAR(255),
   @USERID VARCHAR(255))
AS
BEGIN
 

    INSERT INTO Polls (ID, TITLE, USERID) VALUES (@ID, @TITLE, @USERID);

END;



